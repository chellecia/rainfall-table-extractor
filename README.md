# rainfall-table-extractor
A Streamlit app that extracts, cleans, and visualizes 10-year monthly rainfall tables from images using OCR (Gemini). Generates JSON outputs and rainfall plots automatically.
